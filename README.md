all of below must run from root dir options_amm_poc

setting up virtual env and locally installing FE621 python package

```{bash}
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install -e .
```
